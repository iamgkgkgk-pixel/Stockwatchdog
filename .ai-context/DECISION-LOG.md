# 📜 关键决策日志

> 每个重大决策都记录了 **Why（为什么）** 和 **⚠️ 后果（如果反悔会怎样）**。
> 新 AI 请先通读此文档，避免重蹈覆辙。

---

## DEC-001: 使用方案B统一基准（本地 calcPercentile 替代 API pePercentile）

- **日期**: 2026-03-22
- **背景**: 
  - 实时信号用蛋卷基金 API 返回的 `pePercentile`（基于全市场 10 年+数据），恒生科技 PE=25.8 → 分位 55.2%
  - 历史回算用 `calcPercentile(pe, historicalPE)`（仅 JSON 中 63 个月数据），同一 PE → 分位 77.8%
  - 同一个 PE 值，两套基准差 22.6 个百分点 → 综合评分差 9 分 → 信号级别可能跳两级
- **决策**: 统一使用 `calcPercentile(当前PE, JSON全量PE)` 计算分位，放弃 API pePercentile
- **影响**: 所有分位计算走同一条路径（calcPercentile），历史走势与实时信号 100% 一致
- **⚠️ 不可逆转**: 如果改回 API 分位，历史走势会再次出现断崖式不一致

---

## DEC-002: quality 维度权重从 5% 升至 10%

- **日期**: 2026-03-22
- **背景**: 最初 quality 仅 5%，对总分影响微乎其微，形同虚设
- **决策**: 升至 10%，让 ROE+PB 修正有实际影响力
- **影响**: 所有 ETF 的 dimWeights 中 quality 统一为 10（黄金/商品/债券除外）

---

## DEC-003: 历史走势图 marketTemp 统一为 50

- **日期**: 2026-03-22
- **背景**:
  - 历史月份无法获取真实的 CNN F&G 或 A 股广度数据
  - 如果 marketTemp 设为 null，sentiment 维度被跳过（权重 20-25%），历史月份只用 75-80 权重计算，当前月用 100 权重 → 基准不同
  - 如果部分月份有 marketTemp、部分没有 → 走势图曲线断裂
- **决策**: 所有历史月份 marketTemp 统一为 50（中性），走势图只反映估值+安全边际的变化趋势
- **影响**: 走势图中 sentiment 维度对所有月份贡献一致（50 分），消除了基准差异
- **⚠️ 不可逆转**: 如果给不同月份设不同的 marketTemp，走势图曲线会因基准不同而产生虚假的趋势变化

---

## DEC-004: 安全边际从线性公式改为 E/P 倒数公式

- **日期**: 2026-03-22
- **背景**: 原公式 `130 - PE * 2` 对高 PE 行业（科创板 PE 50-80）不公平，PE=80 → 安全分 -30（被 clamp 到 0）
- **决策**: 改为 `E/P = (1/PE) × 100`，然后线性映射到 0-100
  - PE=20 → E/P=5% → ~75 分
  - PE=40 → E/P=2.5% → ~55 分
  - PE=80 → E/P=1.25% → ~30 分
- **影响**: 成长股（科创、创业板、半导体等）的安全边际评分更合理，不再一刀切扣分

---

## DEC-005: 科创创业标的从 159781 切换到 588300

- **日期**: 2026-03-22
- **背景**: 原来用 `159781`（科创创业50ETF 嘉实），蛋卷基金的 `SH000688` 是科创50指数（PE~164），与科创创业50指数（PE~40）完全不同
- **决策**: 切换到 `588300`（招商科创创业50ETF），并将蛋卷 danjuanCode 设为 null（蛋卷无此指数）
- **影响**: PE 数据从 ~164 恢复到正确的 ~40，信号从"强烈卖出"变为合理区间

---

## DEC-006: localStorage 缓存优先级 — JSON 预设 > 缓存

- **日期**: 2026-03-22
- **背景**: 如果 localStorage 中有旧的脏数据（如错误的 PE 值），会覆盖 JSON 预设的正确数据
- **决策**: `normalizeData` 中，JSON 预设数据优先级高于 localStorage 缓存
- **影响**: 即使 localStorage 有数据，也以 JSON 文件为准，localStorage 只在 API 和 JSON 都不可用时才作为最终回退

---

## DEC-007: 自动刷新频率从 60 秒改为 300 秒

- **日期**: 2026-03-22
- **背景**: 60 秒刷新太频繁，蛋卷基金 API 估值数据每天更新一次，无需频繁请求
- **决策**: 改为 300 秒（5 分钟）
- **影响**: 减少 API 请求频率，降低被限流风险

---

## DEC-008: 去除标普500 +10 分美股溢价偏见

- **日期**: 2026-03-22
- **背景**: 最初给标普500 总分 +10 分"美股长牛溢价"，这是人为偏见，可能误导投资决策
- **决策**: 去除溢价加成，所有 ETF 公平评分
- **影响**: 标普500 综合分回到客观水平

---

## DEC-009: 新增综合分历史分位指标

- **日期**: 2026-04-16
- **背景**: 用户想知道"当前综合评分在历史上排第几"，现有走势图只显示分数绝对值，缺乏历史相对位置参考
- **决策**: 
  - 新增 `calcScorePercentileSeries()` 计算历史分位走势
  - 新增 `calcScoreHistoricalPercentile()` 计算当前分在历史中的排名
  - 新增 `getScorePercentileZone()` 判定分位安全等级（历史低位/偏低/中位/偏高/高位）
  - 在 charts.js 新增 `initScorePercentileChart()` 分位走势图
- **影响**: 用户可直观看到"历史上有多少时间比现在更差/更好"

---

## DEC-010: 综合分历史分位图表不混入实时综合分

- **日期**: 2026-04-16
- **背景**: 
  - 第一次修复分位图表不一致时，将实时综合分（含真实 marketTemp）追加到图表曲线末尾
  - 结果医药 ETF 从绿色突变为橙红色（实时分 43.1 vs 图表统一基准分 65.5）
  - 用户反馈："请不要为了4者完全一致，而引入错误的引导！"
- **决策**: 
  - 图表保持纯净（全部基于 marketTemp=50 统一基准）
  - 摘要和标记点使用图表同基准的 `lastSignal.score`，不使用 `currentTotal`（实时综合分）
- **教训**: 数据一致性 > 表面一致性。不同基准的数据混合比标注差异更危险
- **⚠️ 不可逆转**: 混入实时综合分会导致最后一个点与历史数据不可比，用户无法判断趋势

---

## DEC-011: 利差安全边际引入利率环境因子

- **日期**: 2026-03-22
- **背景**: 低利率环境下（国债收益率 < 2%），股债利差天然偏大，安全边际分虚高
- **决策**: 引入利率调节因子 `rateAdj = max(0.7, min(1.0, (bondYield - 1.0) * 0.3 + 0.7))`
  - 国债 < 2% → 系数 0.7（压缩 30%）
  - 国债 > 3% → 系数 1.0（不压缩）
- **影响**: 价值型 ETF（红利低波、沪深300、自由现金流）的安全边际更客观

---

## DEC-012: 混合估值分 — 偏离度 70% + 分位 30%

- **日期**: 2026-03-22
- **背景**: 纯 PE 分位只回答"排第几"，但如果数据样本量小（63个月），分位不够稳定
- **决策**: `calcHybridValuationScore = deviationScore × 0.7 + percentileScore × 0.3`
  - 偏离度（deviation）: 衡量"离均值多远"，需要 peMean 和 peStd
  - 分位（percentile）: 衡量"排第几"，基于全量 PE 历史
- **理论依据**: 芒格"多把尺子"理论 — 两把尺子比一把尺子更可靠
- **影响**: 需要 JSON 数据中提供 `valuationAnchor: { peMean, peStd }`

---

## DEC-013: 数据可信性优先于持续输出信号

- **日期**：2026-09-14
- **背景**：复核发现缺失估值可仅凭情绪产生强买、代理PE与目标历史混算、旧快照刷新日期、采集失败外推历史等问题。
- **决策**：引入字段级 `fieldMeta`（来源、`asOf`、身份、质量），人工数据独立存储；获取/缓存时间不改变观测日期。核心字段缺失、过期或身份不符时暂停买卖与仓位输出。
- **时效策略**：日估值/债息7个自然日，情绪/趋势3日，ROE180日；这是保守的数据可用性限制，不是买卖阈值，长假可能暂停建议。
- **代理**：创业板指仍可作为参考显示，但不再注入创业板50/科创创业50目标历史；采集独立写入 `proxyValuation`。旧混合历史在显示层隔离，不批量删改原始数据。
- **历史**：分位使用按月等权参照；日视图仅展示已有记录，不构造虚拟未来锚点或用未来点插值。保留中性情绪基准，明确是事后重算，不代表当时信号。完整的逐时点回放与真实价格回测仍是后续工作。
- **采集**：仅存带源日期的有效观测，更新current快照；按cn/us/jp明确路由；失败不默认、不外推，force修订保留旧值记录。
- **兼容**：不更改R-02买卖阈值、不重写原始JSON、不迁移框架。旧缓存缺少元信息时只供核对，需要新观测或有日期的手填才能恢复建议。
- **取代范围**：DEC-001的参照升级为同口径月样本；DEC-006固定JSON优先级升级为字段质量及观测日期排序；DEC-010不混入实时综合分的原则保留。

---

## DEC-014: 可参考与可下仓位指令分离，恢复独立估值历史

- **日期**：2026-09-15
- **背景**：用户指出09-14门禁使综合信号几乎全停、历史图只剩少量标的，损失了工具的核心用途。
- **决策**：三态输出。主数据和参照存在时保留有日期、覆盖率及限制的参考评分；完整且可信时沿用正常信号；只有主数据缺失、身份不符等才不可计算。`calculable` 与 `allowed` 不得混用。
- **历史**：主历史卡改为独立PE/债息位置，不再对完整综合分做排名；按有记录月份等权，并列值计半票。PE越低位置越高；不依赖ROE、情绪、股息等历史字段。
- **代理**：依据已核对的配置和数据切换点2026-04-28，把目标旧历史与创业板指新采样分开。代理只用自身样本、不套目标锚点，持续标注短样本与旧来源限制。
- **不变**：原始JSON、真实观测日期、买卖阈值不变；不恢复PE单日涨幅反推、不伪造日线、不让纯情绪产生买入，也不把参考分折算成80%仓位。
- **验收**：除黄金/商品的非估值路径外，全量23个股票/债券必须保留可读分析与独立历史；同时验证未核验/错误数据不会升级为正常仓位建议。
- **取代**：DEC-013的一刀切停分策略；其字段级来源、真实时效、禁止错误混算的原则保留。

---

> **维护规则**: 每次做重大决策（修改算法/数据源/权重/架构）时，在此文件追加新条目。
> 格式: `DEC-XXX: 标题` + 日期 + 背景 + 决策 + 影响 + 不可逆转警告（如适用）。
